name: flake8

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  linter:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.7]
    name: flake8 - python ${{ matrix.python-version }}

    steps:
      - uses: actions/checkout@master
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: install flake8
        run: python -m pip install -q flake8
      - name: flake8 - check ./*py
        run: flake8 ./*py
      - name: flake8 - check python/*py
        run: flake8 python/*py
      - name: flake8 - check nanoaod/*py
        run: flake8 nanoaod/*py
      - name: flake8 - check nanoaod/corrections/*py
        run: flake8 nanoaod/corrections/*py
      - name: flake8 - check nanoaod/config/*py
        run: flake8 --max-line-length=300 nanoaod/config/*py
      - name: flake8 - check delphes/*py
        run: flake8 delphes/*py
      - name: flake8 - check delphes/config/*py
        run: flake8 --max-line-length=300 delphes/config/*py
      - name: flake8 - check tests/*py
        run: flake8 tests/*py
